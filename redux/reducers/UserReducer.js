import {

} from '../types';

const INITIAL_STATE ={

};

export default function(state = INITIAL_STATE, action){
    switch (action.type){


        //put in case for reducer.
        default:
            return state;
    }
}